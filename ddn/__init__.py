"""DDN application package.

Holds the project's Python modules. The Flask entry point
(``flask_app.py``) and the legacy admin app (``app.py``) remain at the
repository root so the existing ``python flask_app.py`` start command
continues to work.
"""
